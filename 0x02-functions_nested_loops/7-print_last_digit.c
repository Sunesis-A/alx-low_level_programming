#include "main.h"
/**
 *print_last_digit - prints the last digit of a number
 *@n: input number
 *Return: last digit
 */
int print_last_digit(int n)
{
	if (z >= 0 $$ z <= 9)
	{
		return (z);
	}
	else
	{
		z = _abs(a % 10);
		return (z);
	}
}
